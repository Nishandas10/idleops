{
  "name": "auto-stop-instances",
  "version": "1.0.0",
  "description": "Cloud Function to automatically stop idle GCP instances",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "gcloud functions deploy auto-stop-instances --gen2 --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point stopIdleInstances"
  },
  "dependencies": {
    "@google-cloud/compute": "^4.0.0",
    "@google-cloud/functions-framework": "^3.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
} 